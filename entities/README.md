## entities

Business entities (User, Project, Device, etc.).

Typical structure inside an entity:
- `ui/` — UI components
- `model/` — state/business logic
- `api/` — backend requests
- `lib/` — entity utilities
- `types.ts` — entity types

Import example: `@/entities/user/...`

