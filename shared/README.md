## shared

Common, reusable code with no direct coupling to business entities.

Suggested subfolders:

- `shared/ui` — base UI components (Button, Input, etc.)
- `shared/lib` — utilities/helpers
- `shared/config` — configuration (constants, env mapping)
- `shared/types` — shared types

